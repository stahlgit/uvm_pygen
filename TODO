1. MOVE transaction into sequences_lib folder
2. reference model - (optionally fill with enum)
2.1 add config what architecture it would be:
    - a Seq -> DR -> RM -> Scoceboard
                  -> DUT -> MON ->
    - seq -> DR -> RD -> MON -> scoreboard
      seq -> DR ä-> DUT -> MON -> 
2.2 LEAVE it all , so simulation runs

3. scoreboard
3.1 buffer for comparison 
3.2 analytic fifo (analytic port)
3.3 define in config:
    - aexp only (analytic export )
    - aexp fifo 
3.4 !!! in env analytic port connection to fifo + analytic export


4. add cache for last configs used for automatic detection